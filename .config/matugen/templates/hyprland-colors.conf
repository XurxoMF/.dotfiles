$image = {{image}}
<* for name, value in colors *>
${{name}} = rgba({{value.dark.hex_stripped}}ff)
<* endfor *>
<* for name, value in base16 *>
${{name}} = rgba({{value.dark.hex_stripped}}ff)
<* endfor *>
